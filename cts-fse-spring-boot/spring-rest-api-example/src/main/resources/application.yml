server:  port: 5000spring:  datasource:    url: jdbc:mysql://${RDS_HOSTNAME:localhost}:${RDS_PORT:3306}/${RDS_DB_NAME:ct_db}    password: ${RDS_PASSWORD:password}    username: ${RDS_USERNAME:root}    driver-class-name: com.mysql.cj.jdbc.Driver  jpa:    generate-ddl: true    show-sql: true    properties:      hibernate:        dialect:org.hibernate.dialect.MySQL57Dialect
